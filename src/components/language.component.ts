import {Component, Output} from '@angular/core';

@Component({
	selector: 'language',
	templateUrl: '../../assets/templates/body/language.template.html'
})export class Language{
	// @Output() repeat:Array<number> = [1];
	// getItirator(){
	// 	return this.repeat;
	// }
	// setItirator

	/*
		+ set an event emitter for an itirator so it can be 
		updated from personalInformation
	*/
}